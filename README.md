sagas example
==============

一个使用saga模式实现分布式场景事务一致性的例子，多种编程语言提供grpc服务，共同构成saga事务


# TODO

* 增加`docker-compose.yml`文件从而可以更快启动例子
* saga store增加基于数据库的实现
* 独立的saga worker server服务的实现
* 增加基于注解的saga service和动态定义的saga从而可以更方便写saga业务代码
* 更多编程语言的例子
